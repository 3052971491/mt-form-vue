/** 设置按钮形状 */
export enum ButtonShape {
    DEFAULT = 'default',
    CIRCLE = 'circle',
    ROUND = 'round',
}

/** 设置按钮类型 */
// 'primary' | 'ghost' | 'dashed' | 'link' | 'text' | 'default'
export enum ButtonType {
    DEFAULT = 'default',
    PRIMARY = 'primary',
    GHOST = 'ghost',
    DASHED = 'dashed',
    LINK = 'link',
    TEXT = 'text',
}