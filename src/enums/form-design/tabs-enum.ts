/** 枚举-页签的基本样式 */
export enum Type {
    LINE = 'line',
    CARD = 'card',
    EDITABLE_CARD = 'editable-card',
}

/** 枚举-页签位置 */
export enum TabPosition {
    TOP = 'top',
    RIGHT = 'right',
    BOTTOM = 'bottom',
    LEFT = 'left'
}